// This page was initially used as a standalone test UI for AI card generation.
// The product decision is now to use AI only in the context of a specific deck,
// so the actual UI has been moved into: src/app/(app)/decks/[deckId]/page.tsx.
// We keep this file as a no-op placeholder to avoid dead routes or confusion.

export default function DeprecatedAIGeneratePage() {
  return null;
}
